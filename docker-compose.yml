version: "3.8"

services:
  redis:
    image: redis:7-alpine
    container_name: rita-redis
    command: ["redis-server","--appendonly","yes","--save","60","1000"]
    ports:
      - "6379:6379"
    volumes:
      - rita-redis-data:/data
    restart: unless-stopped

volumes:
  rita-redis-data:
